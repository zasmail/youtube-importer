{{#paper-toolbar}}
  {{#paper-toolbar-tools}}

    <!-- {{main-menu reRoute='reRoute'}} -->

    <h2>
      Data Grabber
    </h2>
    <span class="flex"></span>
    <!-- {{#paper-button iconButton=true}}
      {{paper-icon "favorite"}}
    {{/paper-button}} -->
    {{#paper-button iconButton=true onClick='toDb'}}
      {{paper-icon "storage"}}
    {{/paper-button}}
  {{/paper-toolbar-tools}}
{{/paper-toolbar}}
{{facebook-card token=token}}
{{grab-data}}
<!-- {{side-nav token=token}} -->
